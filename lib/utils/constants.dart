import 'package:flutter/material.dart';

const primary = Color(0xFF4927CF);
const secondary = Color(0xFF7659E9);
const tertiary = Color(0xFFFFB800);
const accentText = Color(0xFF452A11);
const leadearBoardBtnColor = Color(0x40FFFFFF);

const pageGradient = RadialGradient(
  center: Alignment(0, 0.5),
  radius: 0.8,
  focalRadius: 0.8,
  colors: [
    secondary,
    primary,
  ],
);
